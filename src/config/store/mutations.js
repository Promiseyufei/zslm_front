export const mutations= {
	//修改变量
	setPassword(state, p) {
		state.setPassword = p;
		// console.log(state.setPassword);
	},
	setTestcode(state, t) {
		state.setTestcode = t;
		// console.log(state.setTestCode);
	}
    
}